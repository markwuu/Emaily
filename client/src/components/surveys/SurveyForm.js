//SurveyForm shows a form for a user to add input
import React from 'react';
import { Form, Field } from 'react-final-form'

const SurveyForm = () => {
    return (
        <div>
            The Survey Form Component!
        </div>
    )
}

export default SurveyForm;
